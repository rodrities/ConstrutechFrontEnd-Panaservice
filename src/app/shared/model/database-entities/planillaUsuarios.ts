import { PlanillaDTO } from "../database-dto/planillaDTO";
import { UnidadDTO } from "../database-dto/unidadDTO";

export class planillaUsuario{
    planUsuId: number;
    planId: number;
    uproId: number;
    usuId: number;
}

